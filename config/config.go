package config

import "log"

func LoadConfig() {
	// Load configuration from file or environment variables
	// and set the values in the config struct
	log.Println("Loaded configuration")
}
